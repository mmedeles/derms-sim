"""
viz package initializer.

Purpose
-------
Marks the `viz` directory as a package that contains the Dash visualization app.
Left intentionally minimal; presence allows `python -m viz.app` style runs.
"""
