# Existing runtime libs (kept from your uploaded file)
numpy
pandas
dash
plotly

# Recommended additions for next stages (MQTT, attacker, detector, evaluation)
paho-mqtt        # lightweight MQTT client for Python
scikit-learn     # detection models (IsolationForest, LOF)
pyyaml           # for attacker config files (attacks.yml)
